// import { FillFormType, FillFormTypeError } from "./SidebarTypes";

export const hellowold = 'hell'
// //check if an error
// // if no error then run query - get item - refresh page



// export const FormErrorHandler = (form: FillFormType, createError: (err: boolean[], value: string, extraValue?: string | null) => void) => {
//     const Errors: boolean[] = []

//     if (form.clientName === '') { createError(Errors, "clientName") }
//     if (form.clientEmail === '') { createError(Errors, "clientEmail") }
//     if (form.paymentTerms === null) { createError(Errors, "paymentTerms") }
//     if (form.invoiceDate === '') { createError(Errors, "invoiceDate") }
//     if (form.projectDescription === '') { createError(Errors, "projectDescription") }
//     if (form.senderAddress.city === '') { createError(Errors, "senderAddress", "city") }
//     if (form.senderAddress.country === '') { createError(Errors, "senderAddress", "country") }
//     if (form.senderAddress.postCode === '') { createError(Errors, "senderAddress", "postCode") }
//     if (form.senderAddress.streetAddress === '') { createError(Errors, "senderAddress", "streetAddress") }
//     if (form.clientAddress.city === '') { createError(Errors, "senderAddress", "city") }
//     if (form.clientAddress.country === '') { createError(Errors, "senderAddress", "country") }
//     if (form.clientAddress.postCode === '') { createError(Errors, "senderAddress", "postCode") }
//     if (form.clientAddress.streetAddress === '') { createError(Errors, "senderAddress", "streetAddress") }


//     return Errors.length === 0
//     // form?.items.forEach((item) => {
//     //     if (item.itemName === '') { createError(`${item?.itemName}`) }
//     //     if (item.itemPrice === null) { createError(`${item?.itemPrice}`) }
//     //     if (item.itemQuantity === null) { createError(`${item?.itemQuantity}`) }
//     // })
// }